using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class remanager : MonoBehaviour
{
    public int defeatenemis;
    // Start is called before the first frame update
    void Start()
    {
        defeatenemis = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
